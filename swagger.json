{
  "swagger": "2.0",
  "info": {
    "description": "This is the documentation for rainbow tracker backend.",
    "version": "1.0",
    "title": "Rainbow tracker",
    "contact": {
      "email": "bernhardsemils@gmail.com"
    }
  },
  "tags": [
    {
      "name": "authentication",
      "description": "Authentication related routes."
    }
  ],
  "paths": {
    "/auth": {
      "post": {
        "tags": [
          "authentication"
        ],
        "summary": "Perform login.",
        "operationId": "login",
        "consumes": [
          "application/json"
        ],
        "produces": [
          "application/json"
        ],
        "parameters": [
          {
            "in": "body",
            "name": "body",
            "description": "Login information.",
            "required": true,
            "schema": {
              "$ref": "#/definitions/Login"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Succesful login."
          }
        }
      }
    }
  },
  "definitions": {
    "Login": {
      "type": "object",
      "properties": {
        "email": {
          "type": "string"
        },
        "password": {
          "type": "string"
        }
      }
    }
  }
}